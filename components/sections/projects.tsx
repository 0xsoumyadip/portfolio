import { projects } from "@/config/projects";
import { ProjectCard } from "@/components/shared/project-card";
export function Projects() { const [featured, ...remaining] = projects; return <section id="projects" className="section" aria-labelledby="projects-title"><div className="shell"><div className="flex items-end justify-between gap-6"><div><p className="eyebrow">04 / Selected work</p><h2 id="projects-title" className="display mt-4 text-4xl font-semibold md:text-6xl">Things I&apos;ve helped<br />bring into focus.</h2></div><p className="hidden max-w-52 text-sm leading-relaxed text-[var(--muted)] md:block">Sample projects are ready to be replaced with your real case studies.</p></div><div className="mt-12"><ProjectCard project={featured} large /></div><div className="mt-5 grid gap-5 md:grid-cols-2">{remaining.map((project) => <ProjectCard key={project.slug} project={project} />)}</div></div></section>; }
