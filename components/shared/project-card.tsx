import Link from "next/link";
import type { Project } from "@/config/projects";
export function ProjectCard({ project, large = false }: { project: Project; large?: boolean }) { return <Link href={`/projects/${project.slug}`} className="group block border rule p-3 transition-transform hover:-translate-y-1"><div className={`flex aspect-[16/9] items-end p-6 ${large ? "md:aspect-[2/1]" : ""}`} style={{ backgroundColor: project.accent }}><p className="text-sm font-medium">{project.category}</p></div><div className="grid gap-5 p-3 pt-6 md:grid-cols-[1fr_auto]"><div><h3 className="text-2xl font-semibold tracking-[-.045em]">{project.name}<span className="ml-2 inline-block transition-transform group-hover:translate-x-1" aria-hidden="true">↗</span></h3><p className="mt-2 max-w-xl leading-relaxed text-[var(--muted)]">{project.description}</p></div><p className="text-sm text-[var(--muted)]">{project.technologies.slice(0, 2).join(" · ")}</p></div></Link>; }
